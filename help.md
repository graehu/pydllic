# help

``` console
usage: pydllic.py [-h] --dll DLL [files [files ...]]

positional arguments:
  files       files parsed by pydllic

optional arguments:
  -h, --help  show this help message and exit
  --dll DLL   dll to link import with ctypes
```
